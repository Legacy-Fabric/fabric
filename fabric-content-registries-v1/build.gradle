archivesBaseName = "fabric-content-registries-v1"
version = getSubprojectVersion(project, "1.0.2")

dependencies {
    compile project(path: ':fabric-api-base', configuration: 'dev')
	compile project(path: ':fabric-object-builders-v0', configuration: 'dev')
}
